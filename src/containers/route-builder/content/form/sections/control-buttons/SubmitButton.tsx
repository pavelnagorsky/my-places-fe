import { Button } from "@mui/material";

const SubmitButton = () => {
  return (
    <Button variant={"contained"} size={"large"}>
      Сохранить маршрут
    </Button>
  );
};

export default SubmitButton;
